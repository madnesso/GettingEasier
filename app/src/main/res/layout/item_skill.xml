<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    android:background="@drawable/background_style">

    <ImageView
        android:id="@+id/skillImage_imageView"
        android:layout_width="108dp"
        android:layout_height="107dp"
        android:contentDescription="@string/todo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tool:src="@drawable/ic_launcher_background" />

    <TextView
        android:id="@+id/skillName_textView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="6dp"
        android:fontFamily="sans-serif-condensed"
        android:textAlignment="center"
        android:textColor="@android:color/black"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/skillImage_imageView"
        app:layout_constraintTop_toTopOf="@id/skillImage_imageView"
        tool:text="Android Development" />

    <androidx.appcompat.widget.AppCompatRatingBar
        android:id="@+id/skillRate_ratingBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:isIndicator="true"
        android:numStars="5"
        android:scaleX="0.7"
        android:scaleY="0.7"
        android:stepSize="0.1"
        app:layout_constraintBottom_toBottomOf="@id/skillImage_imageView"
        app:layout_constraintEnd_toEndOf="@id/skillName_textView"
        app:layout_constraintStart_toStartOf="@id/skillName_textView"
        tool:rating="3.2" />


</androidx.constraintlayout.widget.ConstraintLayout>